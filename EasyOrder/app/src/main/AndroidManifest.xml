<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.easyorder">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="Easy Order"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        tools:replace="android:label,android:theme">
        <activity android:name=".ResturantManager.Manager_Home_Activity"></activity>
        <activity android:name=".owner.Create_Restaurant"></activity>
        <activity android:name=".owner.Main_owner" />
        <activity android:name=".ResturantCooker.Main_Cooker" />
        <activity android:name=".User.Activity.CartActivity" />
        <activity android:name=".User.Activity.HomeActivity" />
        <activity android:name=".User.Activity.MealDetailActivity" />
        <activity android:name=".User.Activity.ResturantDetailsActivity" />
        <activity android:name=".User.Activity.Reservation_Activity" />
        <activity android:name=".User.Activity.meal_typeActivity" />
        <activity android:name=".User.Activity.MenuActivity" />
        <activity android:name=".User.Activity.CreateAccount" />
        <activity android:name=".User.Activity.MainActivity">

            <!-- android:windowSoftInputMode="stateVisible|adjustPan"> ! -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>